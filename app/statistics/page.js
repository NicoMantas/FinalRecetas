"use client"; /*se debe usar use client para que el componente se renderice en el cliente*/

export default function StatisticsPage() {
  /*se debe exportar el componente*/
  return (
    /*se debe retornar un componente*/
    <div>
      <h1>Componente de estadísticas</h1>
      <p>Esta es la página de estadísticas comprobando el enrutamiento</p>
    </div>
  );
}
